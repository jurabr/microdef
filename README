The Microdef is a very simple structural analysis tool. Ic can be used
for a linear static analysis of 2D frames.

The software is designed to be used in POSIX-compliant operating
environment with the X Window System. It used the Gtk+ library 
(Gtk+ 2.x series is required).

This preliminary version features several bugs and (grid drawing
and snapping problems, ancidental ignoring of forces etc.).
